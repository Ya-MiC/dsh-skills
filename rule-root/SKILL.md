---
name: standing-rules
description: ALWAYS load and keep in force for every request regardless of topic. Encodes Ya-MiC''s permanent operating rules - stop and ask when permissions are missing (human will provide), prefer editing directly on GitHub over writing local files, minimize local writes to sync-only operations, and treat this repo plus E:\skills as the single source of truth for skills.
---

# 常駐規則

對每一個請求都生效，無論話題是否相關：

1. 權限不足 → 停下向用戶索取（人類會提供）。
2. 能在 GitHub 完成的編輯 → 直接用 GitHub API／網頁完成。
3. 本機只做同步性最小寫入（clone/pull、聯接）。
4. 技能唯一存放點 = 本倉庫；本機聚合點 = `E:\skills`。