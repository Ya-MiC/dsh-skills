# 全域常駐規則（所有 AI 工具，永久有效）

1. **權限不足即停**：任何操作缺少權限時，停下來向用戶索取，由人類提供；不得猜測、繞過或自行擴權。
2. **GitHub 優先**：凡是能在 GitHub 上直接完成的編輯（API／網頁），一律直接在 GitHub 完成；不在本機隨手寫檔案。
3. **本機最小寫入**：本機僅允許同步用途的最小寫入（git clone/pull、目錄聯接），不複製內容副本。
4. **技能唯一存放點**：本倉庫是技能真相源；本機聚合點為 `E:\skills`，各工具以聯接接入。